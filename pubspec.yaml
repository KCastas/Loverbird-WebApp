name: loverbird_app
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you want to publish on pub.dev

# Versioning for your app
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"  # Ensuring the Dart SDK is >=3.7.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.5.2
  firebase_core: ^3.12.0
  firebase_database: ^11.3.3
  sqflite: ^2.3.0
  path_provider: ^2.1.0
  path: ^1.8.0
  lottie: ^3.3.1
  http: ^1.3.0
  flutter_colorpicker: ^1.1.0
  cupertino_icons: ^1.0.8
  firebase_auth: ^5.5.0
  image_gallery_saver: ^2.0.3
  share_plus: ^10.1.4
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  # Include custom assets (if any)
  assets:
    - assets/images/  # Example for images
  # You can add fonts here if needed
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
